export function LoadingLayout({ children }: { children: React.ReactNode }) {
    return <div className="loading-layout">{children}</div>;
}

export function MeetingLayout({ children }: { children: React.ReactNode }) {
    return <div className="meeting-layout">{children}</div>;
}
